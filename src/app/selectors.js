export const appState = state => state.app;

export const trialValue = state => state.app.trial;
export const trialAsync = state => state.app.trialAsync;

export const selectedColor = state => state.app.canvas.selectedColor;
export const remoteImageData = state => state.app.canvas.remoteImageData;
